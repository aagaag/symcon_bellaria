# Codex Project Instructions

- After changing project files, synchronize the project with `aagaag/symcon_bellaria` on GitHub.
- Prefer committing focused changes and pushing immediately after the commit.
- If local Git is unavailable, report the blocker and keep the GitHub repository state in mind for the next turn.
- Proceed autonomously on setup, verification, and sync work whenever the environment permits it; only ask when an approval gate or missing user credential blocks progress.
